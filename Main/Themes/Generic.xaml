<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:gd="clr-namespace:Sokoban.View.GameDocsComponents;assembly=GameDocs">

    <!--
    <Style TargetType="{x:Type gd:GameDeskControl}">
        <Setter Property="MinWidth" Value="100" />
        <Setter Property="MinHeight" Value="100" />
    </Style>
    -->
    <!--
    <Style TargetType="{x:Type local:GameDocs}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:GameDocs}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    -->
</ResourceDictionary>
